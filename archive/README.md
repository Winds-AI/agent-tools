# Archive

Extensions that are no longer maintained, kept for reference.

| Extension | Why archived |
| --- | --- |
| `pi-sticky` | Pi 0.84.0+ ships a native fullscreen TUI mode (`pi --tui-mode fullscreen`, or set `tuiMode` in `/settings`) that pins the editor/footer while the transcript scrolls — the same job, without monkey-patching internals. |
